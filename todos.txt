u = F   ->   dot_dot_x,  dot_dot_theta
phi_0 = theta^2 - const
phi = theta^(a1) - theta_max^(a1) - a2 * dot_theta + a3


tianhao todo  
1. implement the evaluation function for the parameter learning
    a. sample many states
    b. check if a safe control exists for each of the state for a set of parameters
    *c. also add a regularization term: estimate the volume of the invariant set + encourage it to be large
2. Set the parameters of CMA-ES
3. Implement phi and phi_grad function
3.5. Also implement method to sample x0 in the invariant set. Recall; Invariant set is defined weirdly for high relative degree sys: phi <=0, phi_0 <=0, phi_0 + c_3 \dot{\phi_0} \leq 0
4. integrate with rollout_benchmark


weiye todo 
1. construct agent class
    1. phi and phi grad function for each class
    2. safecontrol function, (u) -> safe control 
2. unified evaluation platform 
    1. rollout the given agent, by calling the safecontrol function
    2. return a score, metric (violation times? 
3. code ssa ,c1 = 1, c2 =1, c3 = 1
safety index: 
$\beta = (theta^2)^{c_1} - (theta_{max}^2)^{c_1} + c_3 $
phi(x) = (theta^2)^{c_1} - (theta_{max}^2)^{c_1} - c_2 \dot{h}(x) + c_3    
where $c_1 > 0, c_2 > 0, c_3 = 0$ are the parameters.
 
And compute $\dot{h}$ analytically as $\dot{h} = \langle \dot{x}, \nabla_{x} h(x) \rangle = 2 \theta \dot{\theta}$.
    

Miscellaneous todo:
1. Save environment class variables (currently global variables) at the end of each rollout 